// Re-export every store from a single import path: `import { useAuth, useDeals } from '@/state'`
export { useAuth } from './auth';
export { useDeal, useDeals } from './deals';
export { useSeller } from './seller';
export { useWallet } from './wallet';
